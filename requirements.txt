# Core (DeltaV inference)
torch>=2.1
torchvision
transformers==4.57.0        # pinned: supports Qwen3-VL and still exposes DefaultFastImageProcessorKwargs (removed in transformers 5.x)
accelerate>=0.34
einops
numpy
scipy
scikit-image               # SSIM for the TSIM Router similarity pipeline
Pillow
timm
safetensors
tqdm
omegaconf

# Data-processing pipeline
ijson
orjson
smart_open
pandas

# Understanding benchmarks via VLMEvalKit
tabulate

# Optional, performance only:
# flash-attn            # faster attention (build against your CUDA/torch)
# MultiScaleDeformableAttention  # compiled op under tsim_tok/modules/ops (see INSTALL.md);
#                                # a pure-PyTorch fallback is used if not built.

# NOTE: DeltaV/TSIM-Tok training and evaluation dependencies (deepspeed, wandb, tensorboard,
# kornia, lmdb, torch-fidelity, tensorflow, nvidia-cudnn, openai, requests) are not needed for
# inference and were dropped here; they will return with the training/evaluation code.

